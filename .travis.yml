language: c
sudo: false # only for faster builds
script:
  - gcc main.c -o main.o -c
  - gcc linked_list.c -o linked_list.o -c
  - gcc -o main.o linked_list.o

after_success:
  - bash <(curl -s https://codecov.io/bash)
